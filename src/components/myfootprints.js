import React from "react";
import { withRouter } from "react-router-dom";

const CreateFootPrints = (props) => {
  return <h1>Here is the my foot prints page</h1>;
};

export default withRouter(CreateFootPrints);
